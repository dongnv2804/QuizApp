import {createAction} from '@reduxjs/toolkit';

export const getQuestions = createAction('GET_QUESTIONS');
export const addDataAnswer = createAction('ADD_DATA_ANSWER');
export const getResult = createAction('GET_RESULT');
